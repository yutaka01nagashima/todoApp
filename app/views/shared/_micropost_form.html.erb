<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "todoタスクを作ってみる" %>
  </div>
  <%= f.submit "作成", class: "btn btn-primary" %>
<% end %>