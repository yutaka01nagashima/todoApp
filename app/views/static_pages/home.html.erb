<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
     <div class="col-md-8">
      <h3>todoリスト一覧</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>シンプルtodoタスク管理App</h1>

    

    <%= link_to ">>signupしてみる<<", signup_path, class: "btn btn-lg btn-primary" %>
  </div>

  
<% end %>